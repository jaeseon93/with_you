<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="co.withyou.care.patient.findload.mapper.patientCallMapper">

	<!-- 로그인 -->
	<select id="getSelect" parameterType="CallFamilyVO" resultType="CallFamilyVO">
		SELECT * FROM FAMILY WHERE FAMILYNO = #{familyNo}
	</select>

</mapper>
